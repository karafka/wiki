#!/bin/bash
# Test script for Material for MkDocs validation tools

echo "🧪 Testing Material for MkDocs validation tools..."
echo

# Test the standalone validator
echo "📝 Testing standalone validator..."
./utils/validate-mkdocs-compatibility

echo
echo "📋 Testing markdownlint with custom rules..."

# Check if markdownlint-cli2 is available
if command -v markdownlint-cli2 &> /dev/null; then
    # Run on a small subset for testing
    markdownlint-cli2 "Getting-Started.md" "Configuration.md" || true
else
    echo "⚠️  markdownlint-cli2 not found. Install with: npm install -g markdownlint-cli2"
fi

echo
echo "✅ Validation tools test completed!"
echo
echo "💡 Usage tips:"
echo "  - Run './utils/validate-mkdocs-compatibility' for full validation"
echo "  - Run 'markdownlint-cli2' for linting with custom rules"
echo "  - Add these to your CI/CD pipeline or git hooks"